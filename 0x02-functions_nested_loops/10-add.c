#include "main.h"
/**
 * add - Entry point
 *
 * Description: a function that adds two integers and returns the result.
 * @int: the first number
 * @int: the second  numbers to sum
 *
 * Return: Always 0 (Success)
 */
int add(int, int)
{
	int a;
	int b;
	int sum;

	sum = a + b;
	return (sum);
}
