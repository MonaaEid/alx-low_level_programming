#include "main.h"
/**
 * add - Entry point
 *
 * Description: a function that adds two integers and returns the result.
 * @a: the first number
 * @b: the second  numbers to sum
 *
 * Return: Always 0 (Success)
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
